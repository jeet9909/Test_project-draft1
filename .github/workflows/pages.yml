# Disabled - site is now deployed via Netlify
name: Deploy site to GitHub Pages
on:
  workflow_dispatch:
    inputs:
      enable:
        description: 'Type YES to enable'
        required: true
jobs:
  deploy:
    if: github.event.inputs.enable == 'YES'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled"
